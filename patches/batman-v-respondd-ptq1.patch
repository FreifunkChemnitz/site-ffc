diff --git a/package/gluon-mesh-batman-adv/src/respondd.c b/package/gluon-mesh-batman-adv/src/respondd.c
index 60df1523..c0158423 100644
--- a/package/gluon-mesh-batman-adv/src/respondd.c
+++ b/package/gluon-mesh-batman-adv/src/respondd.c
@@ -581,9 +581,8 @@ static struct json_object * ifnames2addrs(struct json_object *interfaces) {
 }
 
 static const enum batadv_nl_attrs parse_orig_list_mandatory[] = {
-	BATADV_ATTR_ORIG_ADDRESS,
 	BATADV_ATTR_NEIGH_ADDRESS,
-	BATADV_ATTR_TQ,
+	BATADV_ATTR_THROUGHPUT,
 	BATADV_ATTR_HARD_IFINDEX,
 	BATADV_ATTR_LAST_SEEN_MSECS,
 };
@@ -594,8 +593,8 @@ static int parse_orig_list_netlink_cb(struct nl_msg *msg, void *arg)
 	struct nlmsghdr *nlh = nlmsg_hdr(msg);
 	struct batadv_nlquery_opts *query_opts = arg;
 	struct genlmsghdr *ghdr;
-	uint8_t *orig;
 	uint8_t *dest;
+	uint32_t throughput;
 	uint8_t tq;
 	uint32_t hardif;
 	uint32_t lastseen;
@@ -605,44 +604,51 @@ static int parse_orig_list_netlink_cb(struct nl_msg *msg, void *arg)
 
 	opts = container_of(query_opts, struct neigh_netlink_opts, query_opts);
 
-	if (!genlmsg_valid_hdr(nlh, 0))
+	if (!genlmsg_valid_hdr(nlh, 0)) {
+		printf("!genlmsg_valid_hdr");
 		return NL_OK;
+	}
 
 	ghdr = nlmsg_data(nlh);
 
-	if (ghdr->cmd != BATADV_CMD_GET_ORIGINATORS)
+	if (ghdr->cmd != BATADV_CMD_GET_NEIGHBORS) {
+		printf("!BATADV_CMD_GET_NEIGHBORS");
 		return NL_OK;
+	}
 
 	if (nla_parse(attrs, BATADV_ATTR_MAX, genlmsg_attrdata(ghdr, 0),
 		      genlmsg_len(ghdr), batadv_netlink_policy))
+	{
+		printf("nla_parse");
 		return NL_OK;
+	}
 
 	if (batadv_nl_missing_attrs(attrs, parse_orig_list_mandatory,
 				    ARRAY_SIZE(parse_orig_list_mandatory)))
+	{
+		printf("batadv_nl_missing_attrs");
 		return NL_OK;
+	}
 
-	if (!attrs[BATADV_ATTR_FLAG_BEST])
-		return NL_OK;
-
-	orig = nla_data(attrs[BATADV_ATTR_ORIG_ADDRESS]);
 	dest = nla_data(attrs[BATADV_ATTR_NEIGH_ADDRESS]);
-	tq = nla_get_u8(attrs[BATADV_ATTR_TQ]);
+	throughput = nla_get_u32(attrs[BATADV_ATTR_THROUGHPUT]);
 	hardif = nla_get_u32(attrs[BATADV_ATTR_HARD_IFINDEX]);
 	lastseen = nla_get_u32(attrs[BATADV_ATTR_LAST_SEEN_MSECS]);
 
-	if (memcmp(orig, dest, 6) != 0)
-		return NL_OK;
-
 	ifname = if_indextoname(hardif, ifname_buf);
-	if (!ifname)
+	if (!ifname) {
+		printf("!ifname");
 		return NL_OK;
+	}
 
 	sprintf(mac1, "%02x:%02x:%02x:%02x:%02x:%02x",
-		orig[0], orig[1], orig[2], orig[3], orig[4], orig[5]);
+		dest[0], dest[1], dest[2], dest[3], dest[4], dest[5]);
 
 	struct json_object *obj = json_object_new_object();
-	if (!obj)
+	if (!obj) {
+		printf("!obj");
 		return NL_OK;
+	}
 
 	struct json_object *interface;
 	if (!json_object_object_get_ex(opts->interfaces, ifname, &interface)) {
@@ -650,6 +656,8 @@ static int parse_orig_list_netlink_cb(struct nl_msg *msg, void *arg)
 		json_object_object_add(opts->interfaces, ifname, interface);
 	}
 
+	tq = throughput >= 32768 ? 255 : throughput < 8192 ? throughput >> 10 : (throughput - 8192) / 100 + 8;
+
 	json_object_object_add(obj, "tq", json_object_new_int(tq));
 	json_object_object_add(obj, "lastseen", json_object_new_double(lastseen / 1000.));
 	json_object_object_add(interface, mac1, obj);
@@ -669,7 +677,7 @@ static struct json_object * get_batadv(void) {
 	if (!opts.interfaces)
 		return NULL;
 
-	ret = batadv_nl_query_common("bat0", BATADV_CMD_GET_ORIGINATORS,
+	ret = batadv_nl_query_common("bat0", BATADV_CMD_GET_NEIGHBORS,
 				     parse_orig_list_netlink_cb, NLM_F_DUMP,
 				     &opts.query_opts);
 	if (ret < 0) {
diff --git a/package/gluon-status-page-api/src/batadv-netlink.c b/package/gluon-status-page-api/src/batadv-netlink.c
index ca965d48..37e60d2b 100644
--- a/package/gluon-status-page-api/src/batadv-netlink.c
+++ b/package/gluon-status-page-api/src/batadv-netlink.c
@@ -50,7 +50,7 @@ struct nla_policy batadv_netlink_policy[NUM_BATADV_ATTR] = {
 	[BATADV_ATTR_NEIGH_ADDRESS]	= { .type = NLA_UNSPEC,
 					    .minlen = ETH_ALEN,
 					    .maxlen = ETH_ALEN },
-	[BATADV_ATTR_TQ]		= { .type = NLA_U8 },
+	[BATADV_ATTR_THROUGHPUT]	= { .type = NLA_U32 },
 };
 
 static int nlquery_error_cb(struct sockaddr_nl *nla __maybe_unused,
diff --git a/package/gluon-status-page-api/src/neighbours-batadv.c b/package/gluon-status-page-api/src/neighbours-batadv.c
index 7d5d0211..31ff47a0 100644
--- a/package/gluon-status-page-api/src/neighbours-batadv.c
+++ b/package/gluon-status-page-api/src/neighbours-batadv.c
@@ -15,9 +15,8 @@ struct neigh_netlink_opts {
 };
 
 static const enum batadv_nl_attrs parse_orig_list_mandatory[] = {
-  BATADV_ATTR_ORIG_ADDRESS,
   BATADV_ATTR_NEIGH_ADDRESS,
-  BATADV_ATTR_TQ,
+  BATADV_ATTR_THROUGHPUT,
   BATADV_ATTR_HARD_IFINDEX,
   BATADV_ATTR_LAST_SEEN_MSECS,
 };
@@ -28,8 +27,8 @@ static int parse_orig_list_netlink_cb(struct nl_msg *msg, void *arg)
   struct nlmsghdr *nlh = nlmsg_hdr(msg);
   struct batadv_nlquery_opts *query_opts = arg;
   struct genlmsghdr *ghdr;
-  uint8_t *orig;
   uint8_t *dest;
+  uint32_t throughput;
   uint8_t tq;
   uint32_t hardif;
   uint32_t lastseen;
@@ -39,44 +38,53 @@ static int parse_orig_list_netlink_cb(struct nl_msg *msg, void *arg)
 
   opts = container_of(query_opts, struct neigh_netlink_opts, query_opts);
 
-  if (!genlmsg_valid_hdr(nlh, 0))
-    return NL_OK;
+  if (!genlmsg_valid_hdr(nlh, 0)) {
+	 printf("!genlmsg_valid_hdr");
+	 return NL_OK;
+  }
 
   ghdr = nlmsg_data(nlh);
 
-  if (ghdr->cmd != BATADV_CMD_GET_ORIGINATORS)
-    return NL_OK;
+  if (ghdr->cmd != BATADV_CMD_GET_NEIGHBORS) {
+	  printf("!BATADV_CMD_GET_NEIGHBORS");
+	  return NL_OK;
+  }
 
   if (nla_parse(attrs, BATADV_ATTR_MAX, genlmsg_attrdata(ghdr, 0),
         genlmsg_len(ghdr), batadv_netlink_policy))
-    return NL_OK;
+  {
+	  printf("nla_parse");
+	  return NL_OK;
+  }
 
   if (batadv_nl_missing_attrs(attrs, parse_orig_list_mandatory,
               ARRAY_SIZE(parse_orig_list_mandatory)))
-    return NL_OK;
-
-  if (!attrs[BATADV_ATTR_FLAG_BEST])
-    return NL_OK;
+  {
+	  printf("batadv_nl_missing_attrs");
+	  return NL_OK;
+  }
 
-  orig = nla_data(attrs[BATADV_ATTR_ORIG_ADDRESS]);
   dest = nla_data(attrs[BATADV_ATTR_NEIGH_ADDRESS]);
-  tq = nla_get_u8(attrs[BATADV_ATTR_TQ]);
+  throughput = nla_get_u32(attrs[BATADV_ATTR_THROUGHPUT]);
   hardif = nla_get_u32(attrs[BATADV_ATTR_HARD_IFINDEX]);
   lastseen = nla_get_u32(attrs[BATADV_ATTR_LAST_SEEN_MSECS]);
 
-  if (memcmp(orig, dest, 6) != 0)
-    return NL_OK;
-
   ifname = if_indextoname(hardif, ifname_buf);
-  if (!ifname)
-    return NL_OK;
+  if (!ifname) {
+	  printf("!ifname");
+	  return NL_OK;
+  }
 
   sprintf(mac1, "%02x:%02x:%02x:%02x:%02x:%02x",
-          orig[0], orig[1], orig[2], orig[3], orig[4], orig[5]);
+          dest[0], dest[1], dest[2], dest[3], dest[4], dest[5]);
 
   struct json_object *neigh = json_object_new_object();
-  if (!neigh)
-    return NL_OK;
+  if (!neigh) {
+	  printf("!neigh");
+	  return NL_OK;
+  }
+
+  tq = throughput >= 32768 ? 255 : throughput < 8192 ? throughput >> 10 : (throughput - 8192) / 100 + 8;
 
   json_object_object_add(neigh, "tq", json_object_new_int(tq));
   json_object_object_add(neigh, "lastseen", json_object_new_double(lastseen / 1000.));
@@ -99,7 +107,7 @@ static json_object *neighbours(void) {
   if (!opts.obj)
     return NULL;
 
-  ret = batadv_nl_query_common("bat0", BATADV_CMD_GET_ORIGINATORS,
+  ret = batadv_nl_query_common("bat0", BATADV_CMD_GET_NEIGHBORS,
                                parse_orig_list_netlink_cb, NLM_F_DUMP,
                                &opts.query_opts);
   if (ret < 0) {
